{
  "name": "electron-window-state-async",
  "version": "1.0.0",
  "description": "Electron window state async store",
  "private": true,
  "main": "built/index.js",
  "typings": "built/index.d.ts",
  "scripts": {
    "test": "electron-mocha test.js",
    "build": "tsc -p ./src",
    "clean": "rimraf ./built",
    "rebuild": "npm run clean && npm run build",
    "doc": "typedoc --theme minimal --out docs --mode file --exclude  **/__test__/*",
    "dist": "tsc -p ./src/tsconfig.prod.json"
  },
  "keywords": ["eLectron", "window", "state", "async", "store"],
  "author": "D10221",
  "license": "MIT",
  "dependencies": {
    "electron-json-storage-async": "git+https://github.com/D10221/electron-json-storage-async.git"
  },
  "devDependencies": {
    "@types/electron": "^1.4.38",
    "@types/mocha": "^2.2.41",
    "@types/node": "^7.0.22",
    "electron-mocha": "^3.4.0"    
  }
}
